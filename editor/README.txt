These are icons used by BUeditor. Note that if sprite support is turned on
(it should be), then updating these icons will not have an immediate effect.
The settings must be resaved at admin/settings/bueditor.

All icons in this directory (not including subdirectories) will be available for
editors that identify this directory as icon path.